services:
  pg1:
    image: oncharterliz/pg-1c-15:latest
    volumes:
      - pg1-data:/var/lib/pgpro/1c-15/data
    ports:
    - "127.0.0.1:5432:5432"
    restart: unless-stopped
    shm_size: "1g"

volumes:
  pg1-data: